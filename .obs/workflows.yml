rebuild:
  steps:
    - trigger_services:
        project: Virtualization
        package: kvm-container
    - rebuild_package:
        project: Virtualization
        package: kvm-container
  filters:
    event: pull_request
    branches:
      only:
        - main

